<p><%=t(:mail_sign_up_title)%> <%= @resource.email %>!</p>

<p><%=t(:mail_sign_up_message)%></p>

<p><%= link_to t(:mail_sign_up_link), confirmation_url(@resource, :confirmation_token => @resource.confirmation_token) %></p>
