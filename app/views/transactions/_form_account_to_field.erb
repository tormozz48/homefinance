<!--account to field-->
<div <% if @transaction.errors[:account_to].present? %>
        class="control-group error"
     <%elsif @transaction.errors.any?%>
        class="control-group success"
     <%else%>
        class="control-group"
     <% end %>>
  <%= label_tag :account_to_id, t('field.transaction.account_to'), :class => 'control-label' %>
  <div class="controls">
    <%= collection_select(:transaction, :account_to_id, @accounts, :id, :name_with_amount,
        {:include_blank => t('common.undefined')},
        {:id => 'account_to', :disabled => !@task_new, :title => t('tooltip.input.transaction.account_to')})%>
  </div>
</div>
